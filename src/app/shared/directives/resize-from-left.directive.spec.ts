import { ResizeFromLeftDirective } from './resize-from-left.directive';

describe('ResizeFromLeftDirective', () => {
  it('should create an instance', () => {
    let el: any;
    const directive = new ResizeFromLeftDirective(el);
    expect(directive).toBeTruthy();
  });
});
