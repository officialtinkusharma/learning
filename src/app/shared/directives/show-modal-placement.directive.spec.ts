import { ShowModalPlacementDirective } from './show-modal-placement.directive';

describe('ShowModalPlacementDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowModalPlacementDirective();
    expect(directive).toBeTruthy();
  });
});
