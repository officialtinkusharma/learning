import { ResizeFromRightDirective } from './resize-from-right.directive';

describe('ResizeFromRightDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeFromRightDirective();
    expect(directive).toBeTruthy();
  });
});
