import { TableValueParserPipe } from './table-value-parser.pipe';

describe('TableValueParserPipe', () => {
  it('create an instance', () => {
    const pipe = new TableValueParserPipe();
    expect(pipe).toBeTruthy();
  });
});
